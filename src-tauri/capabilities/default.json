{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: only our own Rust commands, no plugins exposed to the webview.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
